defmodule UseCases do
end

# Generating same table schema like for activity_logging in different application.
#     Instead of defining in different application and using Projects Repo for transacting.
#     Create a macro with all method to perfomr on that table with that inject Repo as argument.
